# distance-generate ( AOD_DistanceGenerate )

A plugin that can generate distance images from the contours of colored regions.

This is the After Effects plugin **AOD_DistanceGenerate**, which provides the **DistanceGenerate.aex** plugin file for Adobe After Effects.

## Building the Plugin

See the [main README](../../README.md) for instructions on how to build the plugin.
